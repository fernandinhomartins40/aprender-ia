"use client";
import { useMemo, useState } from "react";
type Item={slug:string;termo:string;sinonimos:string[];categoria:string;resumo:string;explicacao:string;fonteNome:string|null;fonteUrl:string|null;relacionadoSlugs:string[]};
export function CentralConhecimento({itens}:{itens:Item[]}){const[q,setQ]=useState("");const[seleto,setSeleto]=useState<Item|null>(null);const lista=useMemo(()=>itens.filter(i=>[i.termo,...i.sinonimos,i.categoria,i.resumo].join(" ").toLocaleLowerCase().includes(q.toLocaleLowerCase())),[itens,q]);return <div className="grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><section><input className="campo" value={q} onChange={e=>setQ(e.target.value)} placeholder="Ex.: O que é BNCC? ou avaliação formativa"/><p className="my-3 text-sm text-tinta-clara">A consulta encontra termos e sinônimos cadastrados e mostra somente conteúdo revisado.</p><div className="space-y-2">{lista.map(i=><button key={i.slug} onClick={()=>setSeleto(i)} className="card w-full text-left hover:border-indigo"><p className="font-titulo font-bold">{i.termo}</p><p className="text-sm text-tinta-clara">{i.categoria} · {i.resumo}</p></button>)}</div></section><section className="card h-fit"><h2 className="font-titulo text-xl font-extrabold">{seleto?.termo??"Escolha um assunto"}</h2>{seleto?<><p className="mt-4 whitespace-pre-line text-tinta">{seleto.explicacao}</p>{seleto.fonteNome&&<p className="mt-5 text-sm text-tinta-clara">Informação oficial: <a className="font-bold text-indigo underline" href={seleto.fonteUrl??undefined} target="_blank" rel="noreferrer">{seleto.fonteNome}</a></p>}</>:<p className="mt-3 text-tinta-clara">Pesquise um termo para ver uma explicação clara, a fonte e assuntos relacionados.</p>}</section></div>}
