/**
 * A apresentação ocupa a tela inteira: sem a sidebar do admin, sem cabeçalho.
 * É o que é projetado para a turma — qualquer moldura da plataforma aparece no
 * telão e rouba espaço do conteúdo.
 */
export default function LayoutApresentacao({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
